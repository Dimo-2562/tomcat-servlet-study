# 11. Spring MVC With External Tomcat

## 목표

Spring Boot 없이 순수 Spring MVC를 외장 톰캣에 배포하는 구조를 이해한다.

## 확인할 것

- [ ] `build.gradle`에 Spring MVC 의존성 추가
- [ ] `WebApplicationInitializer`로 `DispatcherServlet` 등록
- [ ] `@Controller`로 요청 처리
- [ ] `web.xml` 방식과 Java Config 방식 비교

## 배운 내용 정리

여기에 직접 적기.

## 실험 결과

여기에 직접 적기.

## 답해야 하는 질문

1. `WebApplicationInitializer`는 `web.xml`을 어떻게 대체하는가?
2. Spring MVC를 붙였을 때 톰캣과의 경계는 어디인가?