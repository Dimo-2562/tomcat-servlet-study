# 03. Servlet Request Flow

## 목표

요청 하나가 톰캣과 서블릿을 어떻게 통과하는지 이해한다.

## 확인한 것

- [ ] 브라우저 -> 톰캣 -> 서블릿 매핑 -> `doGet()` 흐름
- [ ] `contentType` 설정 이유
- [ ] `PrintWriter` 응답 방식의 한계
- [ ] GET/POST 처리 차이

## 배운 내용 정리

여기에 직접 적기.

## 요청 흐름 써보기

여기에 직접 적기.

## 답해야 하는 질문

1. 톰캣은 URL을 기준으로 어떤 서블릿을 선택하는가?
2. 서블릿은 왜 HTTP 요청/응답 객체를 받는가?
